# config/defaults.yml
# the default configuration
# do not edit this file – a separate file, config.yml, is the one
# just for you to add overrides to
# times are given in milliseconds

# currently, this only controls the variant of Vue.js to send to the
# web app user
production: true
# the address to display in links, etc.
# note: the slash is significant
entry_point: 'localhost:29138/'
port: 29138
request_body_size_limit: 16384 # 1 << 14
token_expiry: 604_800_000 # ms = 7 days
exit_on_module_load_error: true
# passed to bcrypt.hash. changing this value will not break existing
# passwords
password_rounds: 8

# please note that modules are loaded in the given order. below are
# given sensible defaults, but you are welcome to use your own values.
modules: {}
### reading from and saving to a disk file
# modules/disk.js:
#   enabled: true
#   save_interval: 300_000
##### please note that a new backup *filename* is given only every
##### hour; therefore, choosing a shorter interval is nothing but
##### prophylactic. 3600_000 (every hour) is good; 86400_000 (every
##### full day) is, for all intents and purposes, good too
#   backup_interval: 1800_000
### refreshing a few variables after loading the database
# modules/housekeep.js: {}
### updating the database from foreign sources; `sources.yml` contains
### the module-specific configuration
# modules/update.js:
#   enabled: true
#   update_interval: 300_000
### removing sufficiently downvoted entries
# modules/cleanup.js:
#   enabled: true
#   vote_threshold: -3
##### if `users` is absent, all users are subject to entry deletion
#   users: 
#     - mrbot
#     - abbott
### pushing updates to Discord
# modules/announce.js:
#   enabled: true
#   hook: 'https://discordapp.com/api/webooks/here/goes/your/webhook'
